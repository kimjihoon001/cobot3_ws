================================================================
토요일 교육장 체크리스트 — 2026-07-17 작성
================================================================

원칙: 순서를 지킬 것 — 앞 단계가 실패하면 뒤 단계는 무의미하다.
     막히면 그 항목은 [보류]로 적고 다음으로 넘어간다.

     코드 수정은 GPU 머신에서 해도 된다 (거기 VSCode + Claude Code + CLAUDE.md 있음).
     고치고 바로 isaac_python 으로 확인되니 그게 빠르다.
     다만 **양쪽에서 동시에 고치지 말 것** — 갈라지면 병합이 제일 아프다.
     한 세션엔 한 방향만. 되가져와서 dev 에서 pytest + git 하면 된다.
     (특히 protocol.py 는 두 벌이라 한쪽만 고치면 조용히 안 붙는다.
      isaacpjt/tests/test_protocol_mirror.py 가 잡아준다 — dev 에서만 돈다)

현재 Isaac 에서 작동 확인된 것 = 0개.
오늘 목표는 "된다/안 된다"를 알아내는 것이지 완성이 아니다.

★회의 전에 2.5 + 2.7 만이라도 돌릴 것 (25분).★
  에셋이 있으면 "AMR 모델링 2일" 배정이 사라지고, 팔이 안 닿으면 로봇 구성이
  바뀐다. 둘 다 역할 분배를 바꾸는 정보다.


================================================================
0. 출발 전 (집에서)
================================================================

[ ] USB 에 복사 — **워크스페이스 통째로** (15MB)

      rsync -av --exclude='__pycache__' --exclude='.vscode' \
                --exclude='build' --exclude='install' --exclude='log' \
                --exclude='.git' \
            ~/cobot3_ws/  /media/jihoon/7072-AEC1/cobot3_ws/

    * USB 경로는 `lsblk` 로 확인할 것 (이 USB 는 7072-AEC1).

    * isaacpjt/ 만 옮기면 안 된다. GPU 노트북에도 ROS2 터미널이 있어서
      거기서 fsm_node 를 돌릴 수 있으므로 src/ 도 같이 가야 한다.
      colcon 은 워크스페이스 루트에 src/ 가 있어야 도니 구조를 그대로 유지할 것.
    * ★.vscode 를 반드시 제외★ — browse.vc.db 가 1.4GB 다. 넣으면 USB 가 터진다.
    * tomato_assets/ (obj 34개, 14MB) 가 반드시 포함돼야 한다. 없으면 0단계 실패.
    * __pycache__ 는 3.10 으로 만든 거라 3.11 에서 쓰레기다.
    * CLAUDE.md(루트 1개)와 이 파일이 같이 간다. 저쪽 Claude Code 가 그걸 읽는다.

[ ] dev 노트북(이거)도 들고 갈 것 — **5번(다중PC 35점)은 머신 두 대라야 한다.**
    GPU 한 대에서 둘 다 돌리는 건 루프백이라 브리지 검증은 되지만 다중PC 가 아니다.


================================================================
1. GPU 노트북 — 기존 것 이름 바꿔두고 통째로 넣기
================================================================

    저쪽은 00~02 를 **오늘 작업을 모르는 상태에서** 돌렸다. 그 머신 코드가 우리 것과
    다를 수 있다 — 특히 00_convert 는 우리 쪽에 경로 버그가 있었는데 저쪽에선
    성공했다니, 저쪽이 이미 고쳐져 있다는 뜻이다. 그게 우리가 모르는 정보다.

[ ] ★기존 것 이름 변경★ (덮지 않는다. 나란히 둔다)

      cd ~/어딘가
      mv isaacpjt isaacpjt_old

[ ] 우리 것 통째로 복사

      rsync -av /media/jihoon/7072-AEC1/cobot3_ws/  ~/어딘가/cobot3_ws/

    * 이제 isaacpjt_old (저쪽 것) 과 cobot3_ws/isaacpjt (우리 것) 이 나란히 있다.
    * 새 폴더라 --delete 여부가 무의미하다. 안전하다.

[ ] diff — 저쪽이 알던 것을 되찾는다

      diff -r isaacpjt_old/tomatest  cobot3_ws/isaacpjt/tomatest
      diff isaacpjt_old/config/settings.py  cobot3_ws/isaacpjt/config/settings.py
      diff -r isaacpjt_old/scene  cobot3_ws/isaacpjt/scene

    ▶ 00_convert 가 저쪽에서 달랐다면 **왜 달랐는지가 정보다.**
      우리 수정과 비교해 나은 쪽을 남기고 RESULTS.md 에 적을 것.
    ▶ TOMATO_SCALE: 저쪽 0.001(41mm) vs 우리 0.001675(68.7mm).
      **둘 다 근거 없는 [4]다** — 저쪽은 눈으로 봤고 우리는 계산했다.
      품종 규격이 있어야 정해진다. 한쪽을 조용히 버리지 말고 둘 다 적어올 것.

[ ] 생성물만 옮겨온다 (obj 가 안 바뀌어서 예전 USD 가 유효하다)

      cp -r isaacpjt_old/tomatest/tomato_assets_usd  cobot3_ws/isaacpjt/tomatest/
      ls cobot3_ws/isaacpjt/tomatest/tomato_assets_usd/*.usd | wc -l    # 34 면 0단계 생략

    ▶ tomato_dataset/ 은 **가져오지 않는다.** 41mm 로 만들어져서 못 쓴다.
      02_generate_dataset.py 가 TOMATO_SCALE=0.001 을 따로 박고 있었다 (오늘 고침).

[ ] 확인 후 isaacpjt_old 는 남겨둘 것. 지우지 말 것 — 나중에 뭐가 나올지 모른다.


================================================================
1.5. 환경 확인 (5분)
================================================================

[ ] Isaac 셸을 연다. **여기서 /opt/ros/humble 을 source 하지 말 것.**
    시스템 librcl.so 와 브리지 내부 librcl.so 가 같이 올라오면 심볼 충돌난다.

[ ] 인터프리터 확인 — 3.11 이어야 한다

      isaac_python -c "import sys; print(sys.version)"

    * 3.10 이 나오면 alias 가 시스템 python 을 가리키는 것. 고치고 시작.

[ ] rclpy 가 없어야 정상 (있으면 환경 가정이 틀린 것)

      isaac_python -c "import rclpy" ; # ModuleNotFoundError 가 나와야 정상


================================================================
2. 0단계 — 토마토 OBJ → USD 변환  ★반드시 먼저★
================================================================

    이걸 안 하면 1·2·4·5단계가 전부 죽는다.

    ※ 1번에서 isaacpjt_old 의 USD 를 이미 옮겨왔다면 **이 단계는 건너뛴다.**
      (obj 가 안 바뀌어서 예전 USD 가 그대로 유효하다)

[ ] cd ~/어딘가/cobot3_ws/isaacpjt

[ ] ls tomatest/tomato_assets_usd/*.usd | wc -l      # 34 면 2.5 로 건너뛴다

[ ] (34 가 아닐 때만) isaac_python tomatest/00_convert_obj_to_usd.py

    기대 출력:
      입력 : ~/.../isaacpjt/tomato_assets
      출력 : ~/.../isaacpjt/tomatest/tomato_assets_usd
      [OK] tomato_ripe_01.usd
      ...
      변환: 성공 34 / 전체 34 -> ~/.../tomato_assets_usd

    ▶ "obj 가 하나도 없다" → tomato_assets/ 를 안 옮긴 것. 0단계로.
    ▶ [FAIL] 이 뜨면 omni.kit.asset_converter 확장이 켜져 있는지 확인.
    ▶ 성공 개수 < 34 → 실패한 파일명 적어두고 일단 진행 (1개만 있어도 스파이크는 됨).

    기록: 성공 ____ / 34


================================================================
2.5. ★최우선★ 스파이크 03: 에셋 확인 (5분) — 회의 전에 돌릴 것
================================================================

    역할 분배를 이걸 모르고 하면 "AMR 2일" 이 실제로 5일이 된다.

[ ] isaac_python spikes/03_asset_check.py

    확인할 것:
      [ ] ForkliftB/C 있나        -> 있으면 AMR 모델링 배정이 통째로 사라짐
      [ ] Ridgeback 있나          -> 베이스 폭이 조간 1.5m 를 통과하나
      [ ] UR10e 있나              -> 기본 RidgebackUr(UR5)는 도달 0.4m 부족
      [ ] Robotiq 2F-85 있나      -> 커터는 어차피 직접 붙임

    기록: 있는 것 ____________________  없는 것 ____________________


================================================================
2.7. ★최우선 2★ 스파이크 04: 로봇 배치 + 도달성 (20분)
================================================================

    25점(로봇제어)의 전제. 팔이 과실에 안 닿으면 온실 치수나 로봇 구성이 바뀐다.
    선행: 2.5 에서 에셋이 있어야 함.

[ ] isaac_python spikes/04_robot_place.py --gui

    놓이는 것: 수확 MM(Ridgeback+UR10e+Robotiq+커터) / 운반 AMR(ForkliftB)
              과실 마커 4개 — 0.5m(초록) / 중간(주황) / 1.4m(빨강) / 반대쪽(보라)

    ★팔을 손으로 끌어서 마커에 닿는지 보는 게 제일 빠르다.★
    (IK 자동 판정은 Lula/RMPflow 필요 — 로봇 확정 후에 할 일)

    [ ] 빨강(1.4m)에 닿나       -> UR5 면 여기서 막힌다. UR10e 로 바꾼 이유
    [ ] 초록(0.5m)까지 내려가나
    [ ] 보라(반대쪽 이랑)도 닿나 -> 통로 중앙에서 양쪽 vs 한쪽씩 도는지 결정
    [ ] 베이스가 통로 1.5m 안에 들어가나
    [ ] 커터가 그리퍼 위에 붙었나
    [ ] 지게차 포크가 승강하나

    ▶ 조립 실패(FileNotFoundError) -> 에셋 경로가 다른 것.
       spikes/03 출력의 실제 경로를 config/settings.py RobotAssetConfig 에 추가.
    ▶ 그리퍼가 안 붙음 -> 팔 끝 프레임 이름이 다른 것.
       harvester.py 의 _TOOL_CANDIDATES 에 추가.
    ▶ 포크 승강 조인트 못 찾음 -> transporter.py 의 _LIFT_CANDIDATES 에 추가.

    기록: 1.4m 닿나 ____  0.5m ____  반대쪽 ____  통로 통과 ____


================================================================
3. 1단계 — 스파이크 01: 마찰 파지  ★프로젝트 성립 여부★
================================================================

    이게 안 되면 수확 방식 자체를 다시 정해야 한다. 그래서 제일 먼저.
    씬도 FSM 도 로봇도 안 쓴다. 판 두 장 + 토마토 하나.

[ ] isaac_python spikes/01_grasp_feasibility.py

    (눈으로 보려면 --gui, 한 조합만 보려면 --mu 0.9 --force 20)

    기대 출력: μ x 파지력 표 + 판정
      - 최소 성공 파지력 (하한)
      - 손상 압력 90kPa x 패드면적 = 상한 표
      - "그리퍼 선정 기준: N 이상, 패드 cm² 이상"

    ▶ 전부 "유지" → 통과. 하한 값을 적는다.
    ▶ 전부 미끄러짐 → 힘 문제가 아니다 (산술 하한이 0.65N 뿐이다).
        스크립트가 확인할 항목을 찍어준다:
          - solver iteration (기준선: 1/60s, 8회 — Find the Fruit 논문)
          - convexHull 이 과실을 너무 둥글게 만들어 접촉이 점에 가까운가
          - contact offset / rest offset
        30분 넘게 안 되면 [보류] 적고 2단계로. 집에서 다시 본다.
    ▶ API 오류 (DriveAPI / PrismaticJoint) → 검증 못 한 코드다. 오류 전문 적어올 것.

    기록: 최소 성공 파지력 ____ N   (mu=0.9 에서 ____ N)
          mu 몇 이상에서 성립? ____
          → 이게 "마찰계수 출처 없음"을 민감도 분석으로 대체하는 근거가 된다.


================================================================
4. 2단계 — 스파이크 02: 꽃자루 401개 부하
================================================================

    씬 설계 A안(전부 dynamic+조인트) vs B안(하이브리드)을 스크립트가 판정한다.
    아무도 과실 400개를 조인트로 매달아본 적이 없다 → 재봐야 안다.

[ ] isaac_python spikes/02_pedicel_feasibility.py

    기대 출력: 개수(50/100/200/401) x 스텝시간/여유/지터 표 + 판정
      - 스텝 < 8ms, 지터 < 1mm, Play/Stop 재현 < 1mm  → A안
      - 물리는 맞는데 느림 → B안
      - 지터/재현 실패 → 부하 문제 아님. 확인 항목이 출력됨.

    ▶ 절단 낙하가 0cm → 조인트가 안 붙었거나 jointEnabled 가 안 먹은 것.

    기록: 401개 스텝시간 ____ ms   지터 ____ mm   재현 ____ mm
          판정: A안 / B안 / 물리불안정


================================================================
5. 3단계 — verify.py (씬 코드 미검증 항목)
================================================================

[ ] isaac_python verify.py          (창 보려면 --gui)

    [OK]/[FAIL]/[SKIP] 로 찍힌다. FAIL 만 적어온다.

    특히 볼 것:
      [ ] TOMATO_SCALE 자동측정 — 집에서 obj 재서 0.001675 로 정했다.
          verify 가 다른 값을 주면 어느 쪽이 맞는지 확인 필요.
      [ ] semantics import 경로 (isaacsim.* vs omni.isaac.*)
      [ ] 과실 401개 실제 FPS
      [ ] convexHull 근사가 토마토에 적절한지
      [ ] ROS2 브리지 확장 enable 여부  ← 5단계 선행조건

    기록: FAIL 항목 ____________________


================================================================
6. 4단계 — 씬 실물 (ROS 없이)
================================================================

[ ] isaac_python main.py --no-ros

    기대: 온실 + 4줄x33그루 + 과실 약 401개가 뜬다.
          콘솔에 클래스별 개수와 수확/제거 대상 수가 찍힌다.

    눈으로 확인할 것:
      [ ] 토마토 크기가 그럴듯한가 (지름 약 6.9cm 로 맞춰놨다)
      [ ] 과실이 허공에 떠 있는가 → 정상. 꽃자루는 아직 씬에 안 물렸다.
      [ ] Play 눌렀을 때 과실이 안 떨어지는가 (kinematic 이라 안 떨어져야 정상)
      [ ] Play/Stop 반복 시 같은 자리로 돌아오는가  ★재현성 20점★
      [ ] FPS

    기록: FPS ____   이상한 점 ____________________


================================================================
7-A. 5단계 먼저 — GPU 노트북 한 대에서 (루프백)
================================================================

    GPU 노트북에 Isaac 터미널 / ROS2 터미널 alias 가 따로 있다. 둘 다 여기서 돌린다.
    127.0.0.1 이 화이트리스트에 있으므로 한 머신 안에서는 붙는다.

    ※ 이건 **다중PC 가 아니다.** 브리지·FSM·통신이 붙는지만 본다.
      먼저 여기서 붙여놓고 7-B 로 가면, 거기선 네트워크 문제만 남는다.
      순서를 이렇게 해야 문제가 하나씩 나온다.

[ ] 터미널 1 (Isaac alias):  isaac_python main.py     → Play 누른다
[ ] 터미널 2 (ROS2 alias):
      cd ~/cobot3_ws && colcon build          # 패키지 3개
      source install/setup.bash               # ★필수★ 안 하면 import 가 깨진다
                                              #  (harvest_fsm -> harvest_interfaces 의존)
      ros2 topic list                     # /harvester_0/harvest/* 가 보여야 함
      ros2 run harvest_fsm fsm_node

    ★ 절대 한 터미널에서 두 alias 를 섞지 말 것. 심볼 충돌난다.

    ▶ 여기서 안 붙으면 네트워크 문제가 아니라 브리지 문제다. 7-B 로 넘어가지 말 것.

    기록: 붙었나 ____   성공률 ____%


================================================================
7-B. 5단계 — 진짜 다중PC: GPU ↔ dev 노트북  ★교육장에서만★  35점
================================================================

    집(192.168.x)에서는 화이트리스트 때문에 루프백만 된다.
    교육장 10.10.0.x 에서만 검증된다. 오늘 아니면 기회가 없다.
    7-A 가 먼저 통과해야 의미가 있다.

--- GPU 노트북 쪽 (Isaac 터미널) ---

[ ] 네트워크 확인: 10.10.0.x 대역인가?  ip a
[ ] 환경변수 — dev 머신과 반드시 같아야 한다
      ROS_DOMAIN_ID=108
      RMW_IMPLEMENTATION=rmw_fastrtps_cpp
      FASTRTPS_DEFAULT_PROFILES_FILE=$HOME/.config/net_looadtest/fastdds_whitelist.xml
      ROS_LOCALHOST_ONLY  → unset 또는 0 (1 이면 크로스머신 전부 차단)

    * 화이트리스트 xml 이 GPU 노트북에 없으면 dev 머신에서 복사해올 것.
      (127.0.0.1 + 10.10.0.1~5, useBuiltinTransports=false)
    * 도커 브리지(172.17.0.1)가 디스커버리를 가로채는 걸 막는 설정이다.
      연결 안 된다고 화이트리스트를 넓히지 말 것.

[ ] isaac_python main.py

    기대: 씬 + "[Bridge] 노드 타입 ... -> isaacsim.ros2.bridge.ROS2Publisher"
                "[Bridge] 그래프 생성 완료: /World/HarvestBridge"
                "[Bridge] 대기 중 — dev 머신에서 ros2 run harvest_fsm fsm_node"

    ▶ "[Bridge] 생성 실패" + traceback → 씬은 계속 뜬다. traceback 전문 적어올 것.
       이게 GPU 없이 검증 못 한 유일한 코드다(graph.py). 실패 가능성이 제일 높다.
       확인 순서:
         1) ROS2 브리지 확장이 켜져 있나 (isaacsim.ros2.bridge)
         2) 노드 타입명 — 스크립트가 후보 2개씩 시도하고 못 찾으면 알려준다.
            실패하면 실제 타입명을 찾아 NODE_CANDIDATES 에 추가해야 한다.
         3) data 속성 못 찾음 → 제네릭 ROS2Pub/Sub 은 messageName 이 풀린 뒤에
            data 가 생긴다. 한 프레임 돌린 뒤 다시 잡아야 할 수도 있다.

[ ] Play 를 누른다 (브리지는 재생 중에만 돈다)

--- dev 노트북 쪽 (별도 터미널, ROS2 alias) ---

[ ] 같은 환경변수 확인 (특히 ROS_DOMAIN_ID=108)
[ ] cd ~/cobot3_ws && colcon build --packages-select harvest_fsm && source install/setup.bash

[ ] 토픽이 보이는가 — 이게 안 되면 아래는 다 안 된다
      ros2 topic list        →  /harvester_0/harvest/{cmd,status,fruits} 가 보여야 함
      ros2 topic echo /harvester_0/harvest/fruits --once

[ ] ros2 run harvest_fsm fsm_node

    ★성공 판정: 과실이 하나씩 바닥에 떨어진다★
      그리퍼가 없으니 떨어지는 게 정상이다. 그게 곧 통신·FSM·물리 경로가
      전부 붙었다는 증거다.

    기대 로그: idle -> detect -> approach -> grasp -> cut -> place -> ...
              마지막에 "수확 완료 — 성공 N / 포기 N / 낙과 N (성공률 %)"

    ▶ "과실 목록 대기 중" 만 반복 → fruits 토픽이 안 온다. 도메인/화이트리스트 확인.
    ▶ 상태가 approach 에서 멈춤 → 30초 뒤 타임아웃으로 실패 처리된다(정상 동작).
       Isaac 쪽 콘솔에 "[Bridge] 명령 수신" 이 찍히는지 확인.

    기록: 성공률 ____%   사이클타임 ____초/과실   ← 정량검증 35점 원천


================================================================
8. 시간이 남으면
================================================================

[ ] 데이터셋을 **다시** 만들어야 한다 (전에 만든 건 못 쓴다)

    이유: 02_generate_dataset.py 가 TOMATO_SCALE=0.001 을 따로 박고 있었다.
          settings.py 는 0.001675 로 바뀌었다. 전에 만든 데이터셋은 지름 41mm
          토마토이고 실제 씬은 68.7mm 다. 그대로 학습하면 YOLO 는 조용히 학습되고
          mAP 만 낮게 나온다 — 원인 찾기가 최악인 종류의 버그다.
          → 고쳤다. 이제 settings.py 를 import 한다. 데이터셋만 다시 뽑으면 된다.

      rm -rf tomatest/tomato_dataset
      isaac_python tomatest/02_generate_dataset.py   (Replicator 500장)
    → 그 다음 dev 에서 YOLO 변환 (GPU 필요 없음):
      python3 -c "import sys; sys.path.insert(0,'isaacpjt'); \
        from vision.yolo_dataset import convert; \
        convert('isaacpjt/tomatest/tomato_dataset', 'isaacpjt/yolo')"

[ ] Isaac 에셋 브라우저에서 확인 (집에서 못 봄):
      [ ] AMR / 지게차 에셋이 있나
      [ ] RMPflow 기본 제공 로봇 목록
      [ ] 쓸만한 그리퍼 — 파지력 스펙과 패드 면적을 볼 것
          (스파이크 01 이 준 하한 이상, 패드 면적이 상한을 만족하는 것)


================================================================
9. 가져올 것 (집에서 이어서 하려면 이게 있어야 한다)
================================================================

[ ] 스파이크 01 출력 전문 (표 + 판정)        ← 그리퍼 선정의 근거
[ ] 스파이크 02 출력 전문 (표 + A/B 판정)    ← 씬 설계 결정
[ ] verify.py FAIL 항목
[ ] 브리지 실패했으면 traceback 전문
[ ] main.py 콘솔 로그 (FPS, 과실 개수)
[ ] fsm_node 로그 (성공률/사이클타임)
[ ] 씬 스크린샷 몇 장                        ← 발표 자료
[ ] 새로 만든/고친 파일이 있으면 USB 로 되가져올 것

    ※ tomato_assets_usd/ 는 안 가져와도 된다 (00_convert 로 재생성).
      .gitignore 에도 들어있다.


================================================================
10. 팀과 정해야 할 것 (아직 미정 — 나 혼자 못 정함)
================================================================

[ ] 문제 정의 / 목표
[ ] 검증 목표 수치 — 평가지표가 "검증 목표(성공률, 한계)가 정의되어 있다"를 요구
      예: 수확 성공률 80%, 과실당 사이클 15초
[ ] 1차/2차/3차 범위
[ ] 역할 분담 (3명 / RTX 5080 2대 = Isaac 슬롯 2개)
[ ] 수확로봇 ↔ 운반 AMR 인계 방식
      상자 차면 부르나? AMR 이 따라다니나? 몇 개마다 교대?
      → 이게 정해져야 AMR FSM 을 짤 수 있다 (엔진은 이미 있음, 재사용만 하면 됨)
[ ] 로봇 플랫폼 — 아직 확정 아님.
      스파이크 01 이 "필요 파지력 N, 패드 cm²" 를 주면 그걸 기준으로 고를 것.
      순서가 반대가 되면 안 된다 (부품 먼저 정하고 되는지 보기 X)
